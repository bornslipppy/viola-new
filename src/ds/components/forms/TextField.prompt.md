# TextField

Text input / textarea with mono uppercase label and hairline border; focus deepens the border to ink.

```jsx
<TextField label="Email" placeholder="you@domain.com" />
<TextField label="Message" multiline hint="Max 500 chars" />
<TextField label="Wallet" error hint="Invalid address" />
```
