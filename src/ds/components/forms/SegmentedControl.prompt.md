# SegmentedControl

Pill-shaped toggle group of mono options — used for view/mode switches (e.g. Testnet / Mainnet, Builder / User).

```jsx
<SegmentedControl options={["Mainnet","Testnet","Devnet"]} defaultValue="Mainnet" onChange={fn} />
```
