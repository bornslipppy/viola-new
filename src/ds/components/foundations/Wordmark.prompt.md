# Wordmark

The Viola Ventures wordmark. Uses `currentColor`, so set the surrounding text color and it adapts to light or dark surfaces. Size with `height`.

```jsx
<Wordmark height={24} />
<div style={{ color: "var(--color-white)" }}><Wordmark height={32} /></div>
```
