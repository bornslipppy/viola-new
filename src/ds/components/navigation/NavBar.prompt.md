# NavBar

Floating pill navigation bar — the primary site header. Fixed near the top on real pages. Contains the wordmark, dropdown menu labels, and a primary CTA.

```jsx
<NavBar items={["Approach","Portfolio","Team"]} ctaLabel="Connect" />
```
